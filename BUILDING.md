## Hacking on hawtio-core

To hack on hawtio-core locally first run:

* `yarn install`

Then run the app:

* `yarn start`

### Release an update:

* Commit your changes

* `yarn publish`

* `git push && git push --tags`
