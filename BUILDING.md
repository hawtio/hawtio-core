## Hacking on hawtio-core

To hack on hawtio-core locally first run:

* `npm install`

Then run the app:

* `npm start`

### Release an update:

* Commit your changes

* `npm version patch`

* `git push && git push --tags`
